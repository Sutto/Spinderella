#!/usr/bin/env ruby
require 'rubygems'
require File.join(File.dirname(__FILE__), "..", "lib", "snp")

SNP::Application.processing(ARGV) do |a|
  a.banner = "SNP - Salt'N'Pepper v#{SNP::VERSION}"
  a.controller! :server, "Starts a Server, accepting client connections and the like"
end


